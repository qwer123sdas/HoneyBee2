<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">
	
	<!--  652 쪽 -->
	<bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	
	<!-- 로그인 관련 인증 -->
	<security:http>
		<security:csrf disabled="true"/>
		<security:form-login login-page="/meeting/login" always-use-default-target="true" default-target-url="/meeting/main" />
		<security:logout logout-success-url="/meeting/main"/>
	</security:http>
	
	<security:authentication-manager>
		<security:authentication-provider>
			<security:jdbc-user-service 
				data-source-ref="dataSource" 
				users-by-username-query="
					SELECT member_id, pw, enable
					FROM Member
					WHERE Member_id = ?
				"
				authorities-by-username-query="SELECT a.member_id, a.role 
					FROM Auth a 
					JOIN Member m ON a.member_id = m.member_id 
					WHERE m.member_id= ?"
				/>
				
		</security:authentication-provider>
	</security:authentication-manager>
	
</beans>